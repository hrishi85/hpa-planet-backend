import { theme } from './theme';

export default {
  config: {
    theme,
    head: {
      favicon: '/admin/favicon.ico',
    },
    auth: {
      logo: '/admin/logo.svg',
    },
    menu: {
      logo: '/admin/logo.svg',
    },
  },
  bootstrap() {},
};
